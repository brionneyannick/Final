<%= form_with(model: [@company, @claim], html: { class: 'form-horizontal' }) do |f| %>
  <div class="form-group">
    <div class="col-md-12">
      <%= f.text_area :description, class: 'form-control' %>
    </div>
  </div>
  <%= f.submit 'Confirmar', class: 'btn btn-primary' %>
<% end %>
